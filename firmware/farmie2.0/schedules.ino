  // void setSchedules(){
  
  // // create the alarms, to trigger at specific times
  // Alarm.alarmRepeat(0,59,0, turnOnLight);  // 8:30am every day
  // Alarm.alarmRepeat(0,59,0,turnOnFan);  // 5:45pm every day
  // Alarm.alarmRepeat(1,0,0,turnOnWaterPump);  // 5:45pm every day
  // Alarm.alarmRepeat(1,1,0,turnOnFertilizerPump);  // 5:45pm every day



  //   Alarm.alarmRepeat(1,0,0, turnOffLight);  // 8:30am every day
  // Alarm.alarmRepeat(1,2,0,turnOffFan);  // 5:45pm every day
  // Alarm.alarmRepeat(1,2,0,turnOffWaterPump);  // 5:45pm every day
  // Alarm.alarmRepeat(1,3,0,turnOffFertilizerPump);  // 5:45pm every day
  // // Alarm.alarmRepeat(dowSaturday,8,30,30,WeeklyAlarm);  // 8:30:30 every Saturday

  // // Alarm.timerOnce(10, OnceOnly);            // called once after 10 seconds


  // }

 